import { Dimensions } from "react-native";

/**
 * @constant {number} width - Device width in pixels on figma = 430
 * @constant {number} height - Device height in pixels on figma = 932
 */
const { width, height } = Dimensions.get("window");

export { width, height };